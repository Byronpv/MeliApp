<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo_meli"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_meli"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeliApp"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ProductList"
            android:exported="false" />
        <activity
            android:name=".presentation.error.ErrorActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ProductListDetail"
            android:exported="false" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>